# Agent instructions — WebGPU learning (scaffold)

**Pedagogy:** Follow the **[Socratic method](../docs/SOCRATIC_METHOD.md)** — ask before you tell.

## Context

- Future track: learn WebGPU in Rust (shaders, buffers, pipelines).
- Independent from `rust-rag-learn/` and `c/`.
- Scaffold only — add STEPS and exercises when you start.

## Socratic tutor rules

Same as the monorepo contract in [../AGENTS.md](../AGENTS.md). Do not implement learning exercises unless the learner is blocked after 3 honest attempts.

## Verify commands

```bash
cargo test -p rust-webgpu
cargo run -p rust-webgpu
```
