# C side quest

Separate from the Rust RAG tutorial. **Goal:** learn C systems basics, then build
**TinyVM** — a minimal 8-bit emulator.

→ Full roadmap: [docs/EMULATOR.md](docs/EMULATOR.md)

## Quick start

```bash
make -C c test EX=01   # run exercise 1 tests only
make -C c test         # all exercises (stubs fail until you implement them)
make -C c build        # hello-world binary
make -C c clean
```

Work in `exercises/exNN_*.c`. One exercise at a time — don't peek ahead.

**Tutor mode:** AI follows the [Socratic method](../docs/SOCRATIC_METHOD.md) — questions before answers. See [AGENTS.md](AGENTS.md).

## Layout

```
c/
├── exercises/          # your implementations (stubs with TODOs)
│   ├── ex01_bytes.c
│   ├── ...
│   └── ex08_run.c
├── tests/              # one test file per exercise
├── fixtures/           # binary ROMs (generated by `make fixtures`)
├── docs/
│   ├── EMULATOR.md     # ISA spec + north star
│   ├── TOPICS.md
│   └── PROGRESS.md
└── Makefile
```

## Current state

Eleven exercise stubs with failing tests. Start with **ex01** (`reverse_bytes`).

**Pointer & heap track:** ex09–ex11 (can run in parallel after ex01). See [docs/TOPICS.md](docs/TOPICS.md).

## Docs

- [docs/EMULATOR.md](docs/EMULATOR.md) — instruction set, architecture, exercise map
- [docs/TOPICS.md](docs/TOPICS.md) — topic summary
- [docs/PROGRESS.md](docs/PROGRESS.md) — session log
