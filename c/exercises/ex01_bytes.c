#include "ex01_bytes.h"

void reverse_bytes(uint8_t *buf, size_t len) {
    (void)buf;
    (void)len;
    /* TODO: two pointers from each end, swap until they meet */
}

uint16_t sum_bytes(const uint8_t *buf, size_t len) {
    (void)buf;
    (void)len;
    /* TODO: walk the buffer and accumulate */
    return 0;
}
